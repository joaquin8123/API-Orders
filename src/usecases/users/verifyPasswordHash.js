// import { ERRORS_MESSAGES } from '../../constants';

// const makeVerifyPasswordHashUsecase = ({ dependencies }) => ({ params }) => {
//   const { decrypt } = dependencies;
//   const { user, passwordInput } = params;
//   if (passwordInput !== decrypt(user.password))
//     throw new Error(ERRORS_MESSAGES.INVALID_CREDENTIALS);
// };

// export default makeVerifyPasswordHashUsecase;
